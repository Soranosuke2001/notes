# `VIEWS` Commands

- Views are automatically updated when the table is upated

**Creating a view**
```sql
CREATE VIEW <view-name> AS
SELECT <column1-name>, <column2-name>
FROM <table-name>;
```

**Deleting a view**
```sql
DROP VIEW <view-name>;
```
