# MySQL Data Types

Different data types in SQL

## Date and Time

- `DATE`: stores the date
- `TIME`: stores the time
- `DATETIME`: stores the date and time

We can get the current date time by the following functions
- current date: `CURRENT_DATE()`
- current time: `CURRENT_TIME()`
- current datetime: `NOW()` or `CURRENT_DATETIME`

