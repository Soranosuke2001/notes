# `log` Command

**Show the commit history**
```bash
git log
```

**Show the commit history but in a condensed pretty way and visually shows the connections of the branches**
```bash
git log --oneline --graph --all
```


