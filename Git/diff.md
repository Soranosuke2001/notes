# `diff` Command

**Shows the changes of each file that have not been staged yet**
```bash
git diff
```

**Shows the changes of each that have been staged but not yet committed**
```bash
git diff --staged
```
