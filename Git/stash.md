# `stash` Commnads

Saves all current changes that have not been committed yet to a stash
```bash
git stash
```

Applies the most recent stash content to the current source code
```bash
git stash pop
```
